/**
 * This code was AUTOGENERATED using the codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getBytesDecoder,
  getBytesEncoder,
  getStructDecoder,
  getStructEncoder,
  getU8Decoder,
  getU8Encoder,
  transformEncoder,
  type Address,
  type Codec,
  type Decoder,
  type Encoder,
  type IAccountMeta,
  type IAccountSignerMeta,
  type IInstruction,
  type IInstructionWithAccounts,
  type IInstructionWithData,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
  type WritableSignerAccount,
} from '@solana/kit';
import { LUDO_PROGRAM_ADDRESS } from '../programs';
import { getAccountMetaFactory, type ResolvedAccount } from '../shared';

export const ROLL_DICE_DEBUG_DISCRIMINATOR = new Uint8Array([
  118, 115, 227, 230, 100, 160, 161, 197,
]);

export function getRollDiceDebugDiscriminatorBytes() {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    ROLL_DICE_DEBUG_DISCRIMINATOR
  );
}

export type RollDiceDebugInstruction<
  TProgram extends string = typeof LUDO_PROGRAM_ADDRESS,
  TAccountPlayer extends string | IAccountMeta<string> = string,
  TAccountGame extends string | IAccountMeta<string> = string,
  TRemainingAccounts extends readonly IAccountMeta<string>[] = [],
> = IInstruction<TProgram> &
  IInstructionWithData<Uint8Array> &
  IInstructionWithAccounts<
    [
      TAccountPlayer extends string
        ? WritableSignerAccount<TAccountPlayer> &
            IAccountSignerMeta<TAccountPlayer>
        : TAccountPlayer,
      TAccountGame extends string
        ? WritableAccount<TAccountGame>
        : TAccountGame,
      ...TRemainingAccounts,
    ]
  >;

export type RollDiceDebugInstructionData = {
  discriminator: ReadonlyUint8Array;
  roll: number;
};

export type RollDiceDebugInstructionDataArgs = { roll: number };

export function getRollDiceDebugInstructionDataEncoder(): Encoder<RollDiceDebugInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([
      ['discriminator', fixEncoderSize(getBytesEncoder(), 8)],
      ['roll', getU8Encoder()],
    ]),
    (value) => ({ ...value, discriminator: ROLL_DICE_DEBUG_DISCRIMINATOR })
  );
}

export function getRollDiceDebugInstructionDataDecoder(): Decoder<RollDiceDebugInstructionData> {
  return getStructDecoder([
    ['discriminator', fixDecoderSize(getBytesDecoder(), 8)],
    ['roll', getU8Decoder()],
  ]);
}

export function getRollDiceDebugInstructionDataCodec(): Codec<
  RollDiceDebugInstructionDataArgs,
  RollDiceDebugInstructionData
> {
  return combineCodec(
    getRollDiceDebugInstructionDataEncoder(),
    getRollDiceDebugInstructionDataDecoder()
  );
}

export type RollDiceDebugInput<
  TAccountPlayer extends string = string,
  TAccountGame extends string = string,
> = {
  player: TransactionSigner<TAccountPlayer>;
  game: Address<TAccountGame>;
  roll: RollDiceDebugInstructionDataArgs['roll'];
};

export function getRollDiceDebugInstruction<
  TAccountPlayer extends string,
  TAccountGame extends string,
  TProgramAddress extends Address = typeof LUDO_PROGRAM_ADDRESS,
>(
  input: RollDiceDebugInput<TAccountPlayer, TAccountGame>,
  config?: { programAddress?: TProgramAddress }
): RollDiceDebugInstruction<TProgramAddress, TAccountPlayer, TAccountGame> {
  // Program address.
  const programAddress = config?.programAddress ?? LUDO_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    player: { value: input.player ?? null, isWritable: true },
    game: { value: input.game ?? null, isWritable: true },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedAccount
  >;

  // Original args.
  const args = { ...input };

  const getAccountMeta = getAccountMetaFactory(programAddress, 'programId');
  const instruction = {
    accounts: [getAccountMeta(accounts.player), getAccountMeta(accounts.game)],
    programAddress,
    data: getRollDiceDebugInstructionDataEncoder().encode(
      args as RollDiceDebugInstructionDataArgs
    ),
  } as RollDiceDebugInstruction<TProgramAddress, TAccountPlayer, TAccountGame>;

  return instruction;
}

export type ParsedRollDiceDebugInstruction<
  TProgram extends string = typeof LUDO_PROGRAM_ADDRESS,
  TAccountMetas extends readonly IAccountMeta[] = readonly IAccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    player: TAccountMetas[0];
    game: TAccountMetas[1];
  };
  data: RollDiceDebugInstructionData;
};

export function parseRollDiceDebugInstruction<
  TProgram extends string,
  TAccountMetas extends readonly IAccountMeta[],
>(
  instruction: IInstruction<TProgram> &
    IInstructionWithAccounts<TAccountMetas> &
    IInstructionWithData<Uint8Array>
): ParsedRollDiceDebugInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 2) {
    // TODO: Coded error.
    throw new Error('Not enough accounts');
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = instruction.accounts![accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      player: getNextAccount(),
      game: getNextAccount(),
    },
    data: getRollDiceDebugInstructionDataDecoder().decode(instruction.data),
  };
}
