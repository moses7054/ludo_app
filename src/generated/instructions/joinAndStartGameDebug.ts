/**
 * This code was AUTOGENERATED using the codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getBytesDecoder,
  getBytesEncoder,
  getStructDecoder,
  getStructEncoder,
  transformEncoder,
  type Address,
  type Codec,
  type Decoder,
  type Encoder,
  type IAccountMeta,
  type IAccountSignerMeta,
  type IInstruction,
  type IInstructionWithAccounts,
  type IInstructionWithData,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
  type WritableSignerAccount,
} from '@solana/kit';
import { LUDO_PROGRAM_ADDRESS } from '../programs';
import { getAccountMetaFactory, type ResolvedAccount } from '../shared';
import {
  getColorsDecoder,
  getColorsEncoder,
  type Colors,
  type ColorsArgs,
} from '../types';

export const JOIN_AND_START_GAME_DEBUG_DISCRIMINATOR = new Uint8Array([
  33, 67, 157, 125, 231, 2, 154, 167,
]);

export function getJoinAndStartGameDebugDiscriminatorBytes() {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    JOIN_AND_START_GAME_DEBUG_DISCRIMINATOR
  );
}

export type JoinAndStartGameDebugInstruction<
  TProgram extends string = typeof LUDO_PROGRAM_ADDRESS,
  TAccountPlayer extends string | IAccountMeta<string> = string,
  TAccountGame extends string | IAccountMeta<string> = string,
  TRemainingAccounts extends readonly IAccountMeta<string>[] = [],
> = IInstruction<TProgram> &
  IInstructionWithData<Uint8Array> &
  IInstructionWithAccounts<
    [
      TAccountPlayer extends string
        ? WritableSignerAccount<TAccountPlayer> &
            IAccountSignerMeta<TAccountPlayer>
        : TAccountPlayer,
      TAccountGame extends string
        ? WritableAccount<TAccountGame>
        : TAccountGame,
      ...TRemainingAccounts,
    ]
  >;

export type JoinAndStartGameDebugInstructionData = {
  discriminator: ReadonlyUint8Array;
  color: Colors;
  startPlayer: Colors;
};

export type JoinAndStartGameDebugInstructionDataArgs = {
  color: ColorsArgs;
  startPlayer: ColorsArgs;
};

export function getJoinAndStartGameDebugInstructionDataEncoder(): Encoder<JoinAndStartGameDebugInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([
      ['discriminator', fixEncoderSize(getBytesEncoder(), 8)],
      ['color', getColorsEncoder()],
      ['startPlayer', getColorsEncoder()],
    ]),
    (value) => ({
      ...value,
      discriminator: JOIN_AND_START_GAME_DEBUG_DISCRIMINATOR,
    })
  );
}

export function getJoinAndStartGameDebugInstructionDataDecoder(): Decoder<JoinAndStartGameDebugInstructionData> {
  return getStructDecoder([
    ['discriminator', fixDecoderSize(getBytesDecoder(), 8)],
    ['color', getColorsDecoder()],
    ['startPlayer', getColorsDecoder()],
  ]);
}

export function getJoinAndStartGameDebugInstructionDataCodec(): Codec<
  JoinAndStartGameDebugInstructionDataArgs,
  JoinAndStartGameDebugInstructionData
> {
  return combineCodec(
    getJoinAndStartGameDebugInstructionDataEncoder(),
    getJoinAndStartGameDebugInstructionDataDecoder()
  );
}

export type JoinAndStartGameDebugInput<
  TAccountPlayer extends string = string,
  TAccountGame extends string = string,
> = {
  player: TransactionSigner<TAccountPlayer>;
  game: Address<TAccountGame>;
  color: JoinAndStartGameDebugInstructionDataArgs['color'];
  startPlayer: JoinAndStartGameDebugInstructionDataArgs['startPlayer'];
};

export function getJoinAndStartGameDebugInstruction<
  TAccountPlayer extends string,
  TAccountGame extends string,
  TProgramAddress extends Address = typeof LUDO_PROGRAM_ADDRESS,
>(
  input: JoinAndStartGameDebugInput<TAccountPlayer, TAccountGame>,
  config?: { programAddress?: TProgramAddress }
): JoinAndStartGameDebugInstruction<
  TProgramAddress,
  TAccountPlayer,
  TAccountGame
> {
  // Program address.
  const programAddress = config?.programAddress ?? LUDO_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    player: { value: input.player ?? null, isWritable: true },
    game: { value: input.game ?? null, isWritable: true },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedAccount
  >;

  // Original args.
  const args = { ...input };

  const getAccountMeta = getAccountMetaFactory(programAddress, 'programId');
  const instruction = {
    accounts: [getAccountMeta(accounts.player), getAccountMeta(accounts.game)],
    programAddress,
    data: getJoinAndStartGameDebugInstructionDataEncoder().encode(
      args as JoinAndStartGameDebugInstructionDataArgs
    ),
  } as JoinAndStartGameDebugInstruction<
    TProgramAddress,
    TAccountPlayer,
    TAccountGame
  >;

  return instruction;
}

export type ParsedJoinAndStartGameDebugInstruction<
  TProgram extends string = typeof LUDO_PROGRAM_ADDRESS,
  TAccountMetas extends readonly IAccountMeta[] = readonly IAccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    player: TAccountMetas[0];
    game: TAccountMetas[1];
  };
  data: JoinAndStartGameDebugInstructionData;
};

export function parseJoinAndStartGameDebugInstruction<
  TProgram extends string,
  TAccountMetas extends readonly IAccountMeta[],
>(
  instruction: IInstruction<TProgram> &
    IInstructionWithAccounts<TAccountMetas> &
    IInstructionWithData<Uint8Array>
): ParsedJoinAndStartGameDebugInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 2) {
    // TODO: Coded error.
    throw new Error('Not enough accounts');
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = instruction.accounts![accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      player: getNextAccount(),
      game: getNextAccount(),
    },
    data: getJoinAndStartGameDebugInstructionDataDecoder().decode(
      instruction.data
    ),
  };
}
