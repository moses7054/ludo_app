/**
 * This code was AUTOGENERATED using the codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getBytesDecoder,
  getBytesEncoder,
  getProgramDerivedAddress,
  getStructDecoder,
  getStructEncoder,
  getU64Decoder,
  getU64Encoder,
  getU8Decoder,
  getU8Encoder,
  transformEncoder,
  type Address,
  type Codec,
  type Decoder,
  type Encoder,
  type IAccountMeta,
  type IAccountSignerMeta,
  type IInstruction,
  type IInstructionWithAccounts,
  type IInstructionWithData,
  type ReadonlyAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
  type WritableSignerAccount,
} from '@solana/kit';
import { LUDO_PROGRAM_ADDRESS } from '../programs';
import {
  expectSome,
  getAccountMetaFactory,
  type ResolvedAccount,
} from '../shared';
import {
  getColorsDecoder,
  getColorsEncoder,
  type Colors,
  type ColorsArgs,
} from '../types';

export const CREATE_GAME_DISCRIMINATOR = new Uint8Array([
  124, 69, 75, 66, 184, 220, 72, 206,
]);

export function getCreateGameDiscriminatorBytes() {
  return fixEncoderSize(getBytesEncoder(), 8).encode(CREATE_GAME_DISCRIMINATOR);
}

export type CreateGameInstruction<
  TProgram extends string = typeof LUDO_PROGRAM_ADDRESS,
  TAccountPlayer extends string | IAccountMeta<string> = string,
  TAccountGame extends string | IAccountMeta<string> = string,
  TAccountSystemProgram extends
    | string
    | IAccountMeta<string> = '11111111111111111111111111111111',
  TRemainingAccounts extends readonly IAccountMeta<string>[] = [],
> = IInstruction<TProgram> &
  IInstructionWithData<Uint8Array> &
  IInstructionWithAccounts<
    [
      TAccountPlayer extends string
        ? WritableSignerAccount<TAccountPlayer> &
            IAccountSignerMeta<TAccountPlayer>
        : TAccountPlayer,
      TAccountGame extends string
        ? WritableAccount<TAccountGame>
        : TAccountGame,
      TAccountSystemProgram extends string
        ? ReadonlyAccount<TAccountSystemProgram>
        : TAccountSystemProgram,
      ...TRemainingAccounts,
    ]
  >;

export type CreateGameInstructionData = {
  discriminator: ReadonlyUint8Array;
  seed: bigint;
  numPlayers: number;
  color: Colors;
};

export type CreateGameInstructionDataArgs = {
  seed: number | bigint;
  numPlayers: number;
  color: ColorsArgs;
};

export function getCreateGameInstructionDataEncoder(): Encoder<CreateGameInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([
      ['discriminator', fixEncoderSize(getBytesEncoder(), 8)],
      ['seed', getU64Encoder()],
      ['numPlayers', getU8Encoder()],
      ['color', getColorsEncoder()],
    ]),
    (value) => ({ ...value, discriminator: CREATE_GAME_DISCRIMINATOR })
  );
}

export function getCreateGameInstructionDataDecoder(): Decoder<CreateGameInstructionData> {
  return getStructDecoder([
    ['discriminator', fixDecoderSize(getBytesDecoder(), 8)],
    ['seed', getU64Decoder()],
    ['numPlayers', getU8Decoder()],
    ['color', getColorsDecoder()],
  ]);
}

export function getCreateGameInstructionDataCodec(): Codec<
  CreateGameInstructionDataArgs,
  CreateGameInstructionData
> {
  return combineCodec(
    getCreateGameInstructionDataEncoder(),
    getCreateGameInstructionDataDecoder()
  );
}

export type CreateGameAsyncInput<
  TAccountPlayer extends string = string,
  TAccountGame extends string = string,
  TAccountSystemProgram extends string = string,
> = {
  player: TransactionSigner<TAccountPlayer>;
  game?: Address<TAccountGame>;
  systemProgram?: Address<TAccountSystemProgram>;
  seed: CreateGameInstructionDataArgs['seed'];
  numPlayers: CreateGameInstructionDataArgs['numPlayers'];
  color: CreateGameInstructionDataArgs['color'];
};

export async function getCreateGameInstructionAsync<
  TAccountPlayer extends string,
  TAccountGame extends string,
  TAccountSystemProgram extends string,
  TProgramAddress extends Address = typeof LUDO_PROGRAM_ADDRESS,
>(
  input: CreateGameAsyncInput<
    TAccountPlayer,
    TAccountGame,
    TAccountSystemProgram
  >,
  config?: { programAddress?: TProgramAddress }
): Promise<
  CreateGameInstruction<
    TProgramAddress,
    TAccountPlayer,
    TAccountGame,
    TAccountSystemProgram
  >
> {
  // Program address.
  const programAddress = config?.programAddress ?? LUDO_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    player: { value: input.player ?? null, isWritable: true },
    game: { value: input.game ?? null, isWritable: true },
    systemProgram: { value: input.systemProgram ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedAccount
  >;

  // Original args.
  const args = { ...input };

  // Resolve default values.
  if (!accounts.game.value) {
    accounts.game.value = await getProgramDerivedAddress({
      programAddress,
      seeds: [
        getBytesEncoder().encode(
          new Uint8Array([108, 117, 100, 111, 95, 103, 97, 109, 101])
        ),
        getU64Encoder().encode(expectSome(args.seed)),
      ],
    });
  }
  if (!accounts.systemProgram.value) {
    accounts.systemProgram.value =
      '11111111111111111111111111111111' as Address<'11111111111111111111111111111111'>;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, 'programId');
  const instruction = {
    accounts: [
      getAccountMeta(accounts.player),
      getAccountMeta(accounts.game),
      getAccountMeta(accounts.systemProgram),
    ],
    programAddress,
    data: getCreateGameInstructionDataEncoder().encode(
      args as CreateGameInstructionDataArgs
    ),
  } as CreateGameInstruction<
    TProgramAddress,
    TAccountPlayer,
    TAccountGame,
    TAccountSystemProgram
  >;

  return instruction;
}

export type CreateGameInput<
  TAccountPlayer extends string = string,
  TAccountGame extends string = string,
  TAccountSystemProgram extends string = string,
> = {
  player: TransactionSigner<TAccountPlayer>;
  game: Address<TAccountGame>;
  systemProgram?: Address<TAccountSystemProgram>;
  seed: CreateGameInstructionDataArgs['seed'];
  numPlayers: CreateGameInstructionDataArgs['numPlayers'];
  color: CreateGameInstructionDataArgs['color'];
};

export function getCreateGameInstruction<
  TAccountPlayer extends string,
  TAccountGame extends string,
  TAccountSystemProgram extends string,
  TProgramAddress extends Address = typeof LUDO_PROGRAM_ADDRESS,
>(
  input: CreateGameInput<TAccountPlayer, TAccountGame, TAccountSystemProgram>,
  config?: { programAddress?: TProgramAddress }
): CreateGameInstruction<
  TProgramAddress,
  TAccountPlayer,
  TAccountGame,
  TAccountSystemProgram
> {
  // Program address.
  const programAddress = config?.programAddress ?? LUDO_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    player: { value: input.player ?? null, isWritable: true },
    game: { value: input.game ?? null, isWritable: true },
    systemProgram: { value: input.systemProgram ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedAccount
  >;

  // Original args.
  const args = { ...input };

  // Resolve default values.
  if (!accounts.systemProgram.value) {
    accounts.systemProgram.value =
      '11111111111111111111111111111111' as Address<'11111111111111111111111111111111'>;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, 'programId');
  const instruction = {
    accounts: [
      getAccountMeta(accounts.player),
      getAccountMeta(accounts.game),
      getAccountMeta(accounts.systemProgram),
    ],
    programAddress,
    data: getCreateGameInstructionDataEncoder().encode(
      args as CreateGameInstructionDataArgs
    ),
  } as CreateGameInstruction<
    TProgramAddress,
    TAccountPlayer,
    TAccountGame,
    TAccountSystemProgram
  >;

  return instruction;
}

export type ParsedCreateGameInstruction<
  TProgram extends string = typeof LUDO_PROGRAM_ADDRESS,
  TAccountMetas extends readonly IAccountMeta[] = readonly IAccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    player: TAccountMetas[0];
    game: TAccountMetas[1];
    systemProgram: TAccountMetas[2];
  };
  data: CreateGameInstructionData;
};

export function parseCreateGameInstruction<
  TProgram extends string,
  TAccountMetas extends readonly IAccountMeta[],
>(
  instruction: IInstruction<TProgram> &
    IInstructionWithAccounts<TAccountMetas> &
    IInstructionWithData<Uint8Array>
): ParsedCreateGameInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 3) {
    // TODO: Coded error.
    throw new Error('Not enough accounts');
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = instruction.accounts![accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      player: getNextAccount(),
      game: getNextAccount(),
      systemProgram: getNextAccount(),
    },
    data: getCreateGameInstructionDataDecoder().decode(instruction.data),
  };
}
