/**
 * This code was AUTOGENERATED using the codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getBytesDecoder,
  getBytesEncoder,
  getI8Decoder,
  getI8Encoder,
  getStructDecoder,
  getStructEncoder,
  getU8Decoder,
  getU8Encoder,
  transformEncoder,
  type Address,
  type Codec,
  type Decoder,
  type Encoder,
  type IAccountMeta,
  type IAccountSignerMeta,
  type IInstruction,
  type IInstructionWithAccounts,
  type IInstructionWithData,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
  type WritableSignerAccount,
} from '@solana/kit';
import { LUDO_PROGRAM_ADDRESS } from '../programs';
import { getAccountMetaFactory, type ResolvedAccount } from '../shared';
import {
  getColorsDecoder,
  getColorsEncoder,
  type Colors,
  type ColorsArgs,
} from '../types';

export const MOVE_TILE_DEBUG_DISCRIMINATOR = new Uint8Array([
  141, 131, 31, 193, 34, 225, 78, 225,
]);

export function getMoveTileDebugDiscriminatorBytes() {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    MOVE_TILE_DEBUG_DISCRIMINATOR
  );
}

export type MoveTileDebugInstruction<
  TProgram extends string = typeof LUDO_PROGRAM_ADDRESS,
  TAccountPlayer extends string | IAccountMeta<string> = string,
  TAccountGame extends string | IAccountMeta<string> = string,
  TRemainingAccounts extends readonly IAccountMeta<string>[] = [],
> = IInstruction<TProgram> &
  IInstructionWithData<Uint8Array> &
  IInstructionWithAccounts<
    [
      TAccountPlayer extends string
        ? WritableSignerAccount<TAccountPlayer> &
            IAccountSignerMeta<TAccountPlayer>
        : TAccountPlayer,
      TAccountGame extends string
        ? WritableAccount<TAccountGame>
        : TAccountGame,
      ...TRemainingAccounts,
    ]
  >;

export type MoveTileDebugInstructionData = {
  discriminator: ReadonlyUint8Array;
  color: Colors;
  tokenNum: number;
  position: number;
};

export type MoveTileDebugInstructionDataArgs = {
  color: ColorsArgs;
  tokenNum: number;
  position: number;
};

export function getMoveTileDebugInstructionDataEncoder(): Encoder<MoveTileDebugInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([
      ['discriminator', fixEncoderSize(getBytesEncoder(), 8)],
      ['color', getColorsEncoder()],
      ['tokenNum', getU8Encoder()],
      ['position', getI8Encoder()],
    ]),
    (value) => ({ ...value, discriminator: MOVE_TILE_DEBUG_DISCRIMINATOR })
  );
}

export function getMoveTileDebugInstructionDataDecoder(): Decoder<MoveTileDebugInstructionData> {
  return getStructDecoder([
    ['discriminator', fixDecoderSize(getBytesDecoder(), 8)],
    ['color', getColorsDecoder()],
    ['tokenNum', getU8Decoder()],
    ['position', getI8Decoder()],
  ]);
}

export function getMoveTileDebugInstructionDataCodec(): Codec<
  MoveTileDebugInstructionDataArgs,
  MoveTileDebugInstructionData
> {
  return combineCodec(
    getMoveTileDebugInstructionDataEncoder(),
    getMoveTileDebugInstructionDataDecoder()
  );
}

export type MoveTileDebugInput<
  TAccountPlayer extends string = string,
  TAccountGame extends string = string,
> = {
  player: TransactionSigner<TAccountPlayer>;
  game: Address<TAccountGame>;
  color: MoveTileDebugInstructionDataArgs['color'];
  tokenNum: MoveTileDebugInstructionDataArgs['tokenNum'];
  position: MoveTileDebugInstructionDataArgs['position'];
};

export function getMoveTileDebugInstruction<
  TAccountPlayer extends string,
  TAccountGame extends string,
  TProgramAddress extends Address = typeof LUDO_PROGRAM_ADDRESS,
>(
  input: MoveTileDebugInput<TAccountPlayer, TAccountGame>,
  config?: { programAddress?: TProgramAddress }
): MoveTileDebugInstruction<TProgramAddress, TAccountPlayer, TAccountGame> {
  // Program address.
  const programAddress = config?.programAddress ?? LUDO_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    player: { value: input.player ?? null, isWritable: true },
    game: { value: input.game ?? null, isWritable: true },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedAccount
  >;

  // Original args.
  const args = { ...input };

  const getAccountMeta = getAccountMetaFactory(programAddress, 'programId');
  const instruction = {
    accounts: [getAccountMeta(accounts.player), getAccountMeta(accounts.game)],
    programAddress,
    data: getMoveTileDebugInstructionDataEncoder().encode(
      args as MoveTileDebugInstructionDataArgs
    ),
  } as MoveTileDebugInstruction<TProgramAddress, TAccountPlayer, TAccountGame>;

  return instruction;
}

export type ParsedMoveTileDebugInstruction<
  TProgram extends string = typeof LUDO_PROGRAM_ADDRESS,
  TAccountMetas extends readonly IAccountMeta[] = readonly IAccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    player: TAccountMetas[0];
    game: TAccountMetas[1];
  };
  data: MoveTileDebugInstructionData;
};

export function parseMoveTileDebugInstruction<
  TProgram extends string,
  TAccountMetas extends readonly IAccountMeta[],
>(
  instruction: IInstruction<TProgram> &
    IInstructionWithAccounts<TAccountMetas> &
    IInstructionWithData<Uint8Array>
): ParsedMoveTileDebugInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 2) {
    // TODO: Coded error.
    throw new Error('Not enough accounts');
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = instruction.accounts![accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      player: getNextAccount(),
      game: getNextAccount(),
    },
    data: getMoveTileDebugInstructionDataDecoder().decode(instruction.data),
  };
}
